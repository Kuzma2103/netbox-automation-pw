# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# IDE/Editor configurations
.vscode/                 # VS Code settings (optional; exclude if project-specific settings are required)
.DS_Store                # macOS file metadata
*.log                    # Log files

# Videos and traces (if stored separately)
*.webm
*.zip
*.trace
*.png